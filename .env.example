# Copy to .env (which is git-ignored).

HOST=0.0.0.0
PORT=3333

# Datastores (match infra/compose.dev.yaml) — not used by the API yet;
# they come in once Postgres/Redis are wired up.
# DATABASE_URL="postgresql://pollo:pollo@localhost:5432/pollo?schema=public"
# REDIS_URL="redis://localhost:6379"
